name: Publish to PyPI

# Fires when a release is published on GitHub.
# Uses Trusted Publishing (OIDC): no stored tokens or passwords.
# One-time prerequisite: register this repository as a
# "pending publisher" of the dataloom-engine project at
# https://pypi.org/manage/account/publishing/
#   - Project name: dataloom-engine
#   - Owner: dionipadilha / Repository: dataloom
#   - Workflow: publish.yml / Environment: pypi
on:
  release:
    types: [published]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      # Prevents publishing a version different from what the tag announces
      - name: Verify release tag matches pyproject version
        run: |
          version="$(python -c 'import tomllib; print(tomllib.load(open("pyproject.toml", "rb"))["project"]["version"])')"
          if [ "${GITHUB_REF_NAME}" != "v${version}" ]; then
            echo "Release tag '${GITHUB_REF_NAME}' does not match pyproject version '${version}' (expected 'v${version}')." >&2
            exit 1
          fi

      - name: Build sdist and wheel
        run: |
          python -m pip install --upgrade pip build
          python -m build

      - name: Upload distributions
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/

  publish:
    needs: build
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      # Required for Trusted Publishing (OIDC)
      id-token: write
    steps:
      - name: Download distributions
        uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist/

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
